# Follow symlinked config files.
--follow

# Case-insensitive unless the query contains uppercase.
--smart-case

# Keep huge/minified lines from flooding terminal output.
--max-columns=200
--max-columns-preview

# Protect against .git internals if hidden files are searched.
--glob=!.git/*
